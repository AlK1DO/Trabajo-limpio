# WhatsApp Bot
This is a script that runs WhatsApp, scans the QR code, and automatically sends messages. It uses the example.env file where the data is located.

# Dependencies: 
```bash 
pip install playwright python-dotenv
```
Then install the Playwright browsers:
```bash
playwright install
```